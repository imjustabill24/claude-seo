# SharkBite (US/EN) — tracked URLs for SEO drift monitoring.
#
# Line types:
#   * a page URL                 -> tracked directly
#   * "@sitemap <url> [prefix]"  -> auto-expands to every page under [prefix]
#                                   from the site's XML sitemap at run time
#   * "#" comment / blank line   -> ignored
#
# The @sitemap directive means you DON'T list each page: it discovers the
# current pages under /us/en/ from the sitemap every run. Cap the count with
# SEO_SITEMAP_LIMIT (default 100).

# Section root (kept explicit — the /us/en/ prefix below matches descendants,
# not the bare /us/en homepage).
https://www.sharkbite.com/us/en

# Everything under /us/en/ , discovered from the sitemap.
@sitemap https://www.sharkbite.com/ https://www.sharkbite.com/us/en/
